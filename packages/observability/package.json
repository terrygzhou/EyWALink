{
  "name": "@eywalink/observability",
  "version": "0.0.1",
  "private": true,
  "description": "AIOps toolkit: agent health monitoring, alerting, cost tracking, degradation detection, and dashboards for agent fleets",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "pnpm build && node --test 'test/**/*.test.ts' 'test/**/*.test.mjs'",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
